import React from 'react';
import ColorArea from './ColorArea';

export default {
  title: 'Components/ColorArea',
  component: ColorArea,
};

const Template = (args) => <ColorArea {...args} />;

export const Default = Template.bind({});
